# queries/future-implementation

**ATENÇÃO: COMANDO NÃO EXECUTADO. DESTINADO SOMENTE À IMPLEMENTAÇÃO FUTURA POR PROFISSIONAL AUTORIZADO, APÓS APROVAÇÃO E JANELA DE MUDANÇA.**

Corpos JSON de requisições de alteração (ex.: novo `_ilm/policy`, novo
`_index_template`, atualização de regra de alerting) referenciados pelos
scripts em `../../scripts/future-implementation/`. Nenhum arquivo aqui foi
enviado ao cluster.

Status atual: **vazia** — depende das recomendações específicas da Fase 1-5,
ainda não coletadas (ver `../../limitations.md`).
