# scripts/future-rollback

**ATENÇÃO: COMANDO NÃO EXECUTADO. DESTINADO SOMENTE À IMPLEMENTAÇÃO FUTURA POR PROFISSIONAL AUTORIZADO, APÓS APROVAÇÃO E JANELA DE MUDANÇA.**

Scripts de reversão para cada mudança em `../future-implementation/`. Cada
script de implementação `NN-xxx.sh` terá um par `NN-xxx-rollback.sh` aqui,
com o comando exato para restaurar o estado anterior (baseado na evidência
capturada em `../../current-config-backups/` antes da mudança).

Status atual: **vazia** — depende das recomendações específicas da Fase 1-5,
ainda não coletadas (ver `../../limitations.md`).
